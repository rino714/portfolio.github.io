<div class="main users-index">
  <div class="container">
    <% @users.each do |user| %>
      <div class="users-index-item">
        <div class="user-right">
          <%= link_to(user.name, "/CPM-program/users/#{user.id}") %>
        </div>
      </div>
    <% end%>
  </div>
</div>
